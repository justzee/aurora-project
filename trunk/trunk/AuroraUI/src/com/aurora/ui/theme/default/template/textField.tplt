<DIV class="${wrapClass}" id="${id}" >
	<INPUT class="item-textField" atype="field.input" style="width: 147px;" name="${name}" ${style} ${readOnly} autocomplete="off" value="${value}" />
</DIV>
<script>
Aurora.onReady(function(){
	new Aurora.TextField('${id}',${config});
	${events}
})
</script>